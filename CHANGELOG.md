# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [0.6.0] - 2026-03-25

### Added

- Підтримка мультивалютності: довідник курсів валют (НБУ), автоматична конвертація транзакцій у системну валюту, збереження даних конвертації (оригінальна сума, валюта, курс) разом із транзакцією
- Окрема таблиця `INVALID_TRANSACTIONS` для невалідних транзакцій (відсутня валюта, невідомий курс тощо) з відображенням причини в аналітиці по користувачу
- Новий тип кешбеку — кобрендинговий: підтримка налаштувань по типу карти, паралельна робота з дефолтним кешбеком, окремі мобільні ендпоінти для отримання налаштувань та позицій
- Поле `Merchant Name` як додаткова умова матчингу транзакцій по мерчанту (разом з Terminal ID)
- Додаткові поля `terminal_name` та `terminal_location` при додаванні терміналів по мерчанту
- Лічильник загальної кількості завантажених терміналів для кожного мерчанта в розрізі всіх типів кешбеку
- Ендпоінт отримання даних по кешбеку для конкретної транзакції (пошук за ID транзакції)
- Пагінований ендпоінт отримання повної історії кешбеку користувача (продубльований на мобільний gateway)
- Поле `bannerUrl` в мобільному ендпоінті `/cashbacks/positions` (тільки для позицій типу мерчант)
- Поле `accountId` в `CASHBACK_HISTORY` — зберігається рахунок, на який здійснено виведення коштів
- Повернення `cashbackHistoryId` у відповіді ендпоінту `/withdraw` (для підтримки операцій реверсу)
- [Deeployalty] Відправка webhook про вибраний кешбек користувача на Deeployalty при зміні налаштувань кастомера
- Розгортання модуля кешбеків для клієнта «Кліринговий дім»
- Відображення помітки Deeployalty на мерчантах при їх виборі під час створення кешбеку
- Можливість перегляду помилок по рядках при імпорті транзакцій
- Відображення обов'язкових полів у всіх формах системи

### Changed

- [Deeployalty] Оновлено логіку синхронізації терміналів: термінали в розділі «Мерчанти» тільки доповнюються (не видаляються), в блоці мерчанта кешбеку — актуальні термінали поточної кампанії
- Аналітика на фронтенді відображає дати транзакцій у локальному часі (замість UTC)
- Поле `accountId` в ендпоінті `/cashback-histories/withdraw` стало обов'язковим
- Приховано сторінку статистики (дані були захардкоджені, не відображали реальних значень)
- Уніфіковано пагінацію на всіх вкладках з табличним відображенням

### Fixed

- Неможливо змінити налаштування мерчанта Deeployalty для кешбеку на наступний місяць
- Не мінусувався кешбек при рефанді з конвертацією валюти
- 500 помилка при виведенні кешбеку
- 500 помилка при виведенні коштів у клієнта «Кліринговий дім»
- Помилка авторизації на сервері Ощадбанку (дев)
- Помилка підключення бд (дев Ощадбанк)
- Нарахування кобрендингового кешбеку відбувалось без урахування типу картки
- Активний кобрендинговий кешбек на поточний місяць доступний до редагування
- Відсутня можливість вимкнути конвертацію валют при початковому налаштуванні системи
- Кнопка «Зберегти» була активна навіть без змін у налаштуваннях API валют
- UAH-транзакція проходила лише у верхньому регістрі (UPPERCASE)
- Oracle: обмеження `IN()` на 1000 елементів — виправлено обробкою батчами
- Після виходу з облікового запису відбувався редирект на `/setup-database` замість `/login`
- Відсутній редирект на сторінку входу після підтвердження виходу в розділі Settings
- Назви колонок `CreatedAt`, `Parent ID`, `LDAP Filter` не були перекладені українською у відповідних розділах
- Назви валют у розділі «Курс валют» відображались без назви країни в українській локалізації
- Сума в списку транзакцій відображалась без конвертації у системну валюту
- Кнопка «Документація» відкривала головну сторінку замість документації
- Файл терміналів по мерчанту вивантажувався порожнім
- Лічильник терміналів у картці мерчанта враховував термінали з кешбек-налаштувань
- Помилки при роботі з WebPlatform API (Кліринговий дім)

## [0.5.0] - 2026-02-26

### Added
- Налаштування тайм-зони для коректного опрацювання транзакцій
- Додано налаштування DB_ENCRYPT & DB_TRUST_SERVER_CERTIFICATE для MSSQL конекшену
- Вбудова посилання на документацію в інтерфейс адмінки
- Підтримка MD-формату в полі «Опис» по мерчанту та віддача значення на API
- Kafka Enrichment Microservice: інтеграція з Client API, збагачення транзакцій, споживання повідомлень з Kafka
- MapperCheckService для валідації DataSourceMapper перед Kafka-операціями
- Пагінований ендпоінт для історії кешбеків з фільтрами по датах
- Таблиця TRANSACTION_FAILURES та workflow для невдалих транзакцій
- GroupCashbackHistoryItemResponse для спрощеної історії кешбеків у деталях транзакції
- maxAmount на рівні мерчанта
- Поле banner URL для мерчантів
- Короткий опис для мерчантів та груп
- processErrorMessage у відповідях обробки транзакцій

### Changed
- Зміна логіки редагування кешбеку
- [Deeployalty] Забір поля макс сума кешбеку по мерчанту при інтеграції Deeployalty
- [Deeployalty] Крон запуск раз на 1 год для оновлення кешбек-пропозицій
- Видалено CurrentCashbackResponse та пов'язану логіку
- Рефакторинг логіки cashback service для персональних/дефолтних кешбеків
- Рефакторинг обробки refund-транзакцій

### Fixed
- Накладання крон джоб
- Не працює отримання кешбеків за певним типом
- Імпорт транзакцій доступний не адміну
- Невірна прод лінка Deeployalty для інтеграції по кешбекам
- Помилка HTTPS dev stage
- Рада ПРОД: мерчанти відмітились як від Deeployalty
- Реферальна програма доступна не адміну
- Прод рада: відсутні раніше додані користувачі в конфігураторі
- Прод рада: помилка GET при переході на розділ вебхуків
- Відсутня можливість експорту списку кастомерів по персональному кешбеку
- Імпорт транзакцій — спливаюче повідомлення не перекладається при зміні мови інтерфейсу
- Помилки при роботі з WebPlatform API кліринговий дім
- Блокуються поля налаштування мерчанта Deeployalty при його додаванні в дефолтний/персональний кешбек
- Помилка додавання мерчанта в поточний дефолтний кешбек, якщо в ньому був мерчант Deeployalty
- Невірно нарахований кешбек по мерчанту
- Створення персонального кешбеку
- Часовий пояс Deeployalty

## [0.4.0] - 2026-02-12

### Added
- CRUD для типів кешбеку (cashback operation types) в адмінці
- Автоматичну ініціалізацію nv-ключів у Vault при першому запуску
- Імпорт та експорт списку терміналів для кешбеку по мерчанту
- Розділ для завантаження транзакцій в адмінці
- Розділ налаштувань в адмінці з можливістю:
  - оновлювати LDAP-підключення та мапери ролей
  - менеджити вручну створених користувачів
  - змінювати доступи на редагування минулих періодів кешбеків
  - налаштовувати вебхуки
  - додавати нові access keys для Mobile Gateway (авторизація)
  - змінювати налаштування бази даних (не рекомендується після стартового сетапу)
  - додавати нові джерела даних (наприклад, підвантаження транзакцій з Kafka)
- Новий ендпоінт `GET /api/v2/cashback/current` для отримання поточних налаштувань кешбеку, позицій та обраних карткових продуктів в одному запиті

### Changed
- Роути мапінга вебхуків із перенесенням полів у subscriptions
- Валідацію поля "Опис" у мерчантах: дозволені всі спецсимволи
- Видалено deprecated ендпоінти:
  - `GET /customer-cashback/chosen/:id` 
  - `GET /settings`
  - `GET /allowed-cashbacks/positions` 
  - `GET /receipt/transaction/:id`
- Персональний кешбек тепер повністю перекриває дефолтний (раніше відбувався merge дозволених позицій)
- Поле `accountId` в `POST /cashback-histories/withdraw` стало опціональним (раніше було обов'язковим)

### Fixed
- 500 при додаванні персональних кешбеків через бекенд
- Некоректний logout і відсутність редіректу на сторінку авторизації
- Відображення полів % оплати у дефолтних кешбеках при помилках валідації
- LDAP: відсутність групи в Keycloak і недоступність перевірки конекту при оновленні
- Відсутність транзакцій в аналітиці користувача та помилки імпорту транзакцій
- Помилку імпорту терміналів
- Некоректний endDate при створенні кешбеку

## [0.3.0] - 2026-02-03

### Added
- Vault інтеграцію з середовищами, сервісами, імпортом секретів і документацією
- Діплоялті інтеграція - підвантажуємо мерчантів, термінали та кешбек програми
- Kafka бібліотеку, менеджер з'єднань і керування консюмерами
- Модулі DataSources та DataSourceMappers з CRUD, тестовими ендпоінтами та schema analyzer
- Повний стек Webhooks: підписки, мапери, події, delivery logs і payload шаблони
- Конфігураційний модуль Keycloak з LDAP-ендпоінтами, групами/ролями та статистикою
- MinIO модуль і скрипти ініціалізації buckets/policies
- Mobile access keys модуль і ендпоінти

### Changed
- CI/CD пайплайни та GitHub Actions для збірки/публікації образів з тегом версії
- Dockerfile та docker-compose для локальних і prod оточень, nginx/filebeat конфіги
- Динамічну БД: TS seeds, нові міграції, bulk-json-save і SQL-хелпери
- [DB] Оновлення схеми та міграцій (таблиці/колонки/індекси):
  - Нові таблиці:
    - `DATA_SOURCES`
    - `DATA_SOURCE_MAPPERS`
  - `TRANSACTIONS`:
    - додано колонку `DATA_SOURCE_ID` (звʼязок з `DATA_SOURCES`)
  - `CUSTOMER`:
    - додано колонку `ADDED_BONUSES` (decimal, default 0)
  - `RECEIPTS`:
    - видалено retry-колонки: `RETRY_ATTEMPTS`, `NEXT_RETRY_AT`, `RETRY_ERROR_MESSAGE`, `RETRY_STATUS`
    - видалено індекс `IDX_RECEIPTS_RETRY_STATUS_NEXT_RETRY` (по `RETRY_STATUS`, `NEXT_RETRY_AT`)
  - Webhooks (схема таблиць):
    - `WEBHOOK_SUBSCRIPTIONS`: додано поля авторизації/доставки (`HTTP_METHOD`, `USE_AUTHORIZATION`, `AUTH_TYPE`, `AUTH_API_KEY`, `AUTH_LOCATION`, `AUTH_FIELD_NAME`)
    - `WEBHOOK_SUBSCRIPTIONS`: додано унікальний індекс `IDX_WEBHOOK_SUBSCRIPTIONS_CALLBACK_URL` (по `CALLBACK_URL`)
    - `WEBHOOK_MAPPERS`: видалено колонки `AUTHORIZATION_TYPE`, `HTTP_METHOD` (перенесено в `WEBHOOK_SUBSCRIPTIONS`)
    - `WEBHOOK_EVENTS`: `DESCRIPTION` → `DESCRIPTION_EN` + додано `NAME_EN`, `NAME_UA`, `DESCRIPTION_UA` (multilang)
  - Міграції:
    - багато старих міграцій видалено/консолідовано, додані нові точкові міграції для змін вище
- Логування й обробку помилок у ключових сервісах (централізовані хендлери)
- Обробку транзакцій і кешбеків: SQL-операції, batch/parallel, гнучкі дати
- Конфігурації Keycloak/Redis/Deeployalty та структуру vault paths для середовищ

### Fixed
- Надійність ініціалізації та логування під час підключень до БД/Redis/Kafka

## [0.2.0] - 2025-12-03

### Added
- Логіка завантаження та обробки файлів зі списком ID клієнтів для персональних кешбеків
- API-методи для додавання та оновлення списку кастомерів у налаштованому кешбеці

### Changed
- UI карткових продуктів змінено з табличного вигляду на карточки
- Актуалізовано фронтенд та підключено методи для персональних кешбеків
- Змінено опис відповіді в `/cashback-histories/withdraw` якщо баланс користувача менший за суму виведення

### Fixed
- Виправлено можливість видалення кешбека на поточний місяць

## [0.1.0] - 2025-11-17

### Added
- Реалізовано базову реферальну програму
- Ендпоінт для завантаження транзакцій в систему
- Можливість видалення кешбека на наступний період
- Пошук термінала та завантаження терміналів з файла у формі мерчанта
- Валідація для форм створення/редагування груп, MCC, карткових продуктів, мерчантів
- Додаткові спроби на пошук транзакції для матчингу чека
- Обмеження на додавання файлів з terminal ID
- Реверс нарахувань при помилці на add-cashback та withdraw

### Changed
- Поле "Назва програми" для реферальних акцій тепер обов'язкове при створенні акційного посилання
- Мінімальна сума по мерчанту тепер обов'язкове поле для персональних кешбеків
- В аналітиці по користувачу дані по кешбекам та реферальній програмі виведено окремими меню
- Приховано недоступні дати при створенні кешбеку
- Приховано список терміналів по мерчанту
- [DevOps] Обмеження зберігання логів у Elasticsearch (Kibana) до 3 місяців
- Виправлено обробку кейсів "Поточний баланс клієнта менше ніж мінімальна сума для виведення бонусів"

### Fixed
- Кастомер більше не може зареєструватись по своєму власному реферальному коду
- Виправлено 500 помилку при реєстрації нового користувача по реферальному коду
- Виправлено помилку "Досягнуто ліміт реєстрацій" при зміні максимальної кількості користувачів на реєстрацію
- Виправлено назву кнопки "Назад" і розділу для програми користувача
- Форма редагування акційного посилання більше не закривається при копіюванні коду
- Виправлено помилку "Досягнуто ліміт реєстрацій" при реєстрації по акційному коду
- Виправлено відображення таблиці з даними по посиланню користувача
- Виправлено відображення суми запрошеного/запрошувача в аналітиці по користувачу
- Заблоковано реєстрацію по неактивному за датами акційному посиланню
- Додано можливість редагування дат для акційного посилання
- Виправлено відкриття календаря при натисканні на неактивну кнопку очищення поля дат
- Виправлено зникнення таблиці даних по посиланнях користувача при повторному натисканні на розділ
- Виправлено повідомлення про помилку при реєстрації по невалідному коду
- Виправлено перевищення рефанду
- Виправлено набір даних при експорті в файл кастомерів по персональному кешбеку